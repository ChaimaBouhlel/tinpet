import UserProfile from '../components/userProfile/userProfile';

const userProfile = () => {
  return (
    <div>
      <h1>Form Page</h1>
      <UserProfile />
    </div>
  );
};

export default userProfile;